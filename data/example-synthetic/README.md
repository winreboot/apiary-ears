# Format example (not a real recording)

These numbers are generated, not measured. They exist so contributors can see the shape
of a valid file. `tools/validate.py` accepts them; no model should be trained on them.
